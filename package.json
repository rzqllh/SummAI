{
  "name": "summai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"cyan,magenta\" \".\\.venv\\Scripts\\uvicorn.exe backend.main:app --reload --port 8000\" \"npm run dev --prefix frontend\"",
    "dev:backend": ".\\.venv\\Scripts\\uvicorn.exe backend.main:app --reload --port 8000",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
